df = spark.createDataFrame(data=[['a','bbbbb','ccc','ddd'],['aaaa','bbb','ccccccc', 'dddd']],schema="col1 string,col2 string,col3 string, col4 string")
df1 = df.select([sf.length(col).alias(col) for col in df.columns])
df1.groupby().max().show()
